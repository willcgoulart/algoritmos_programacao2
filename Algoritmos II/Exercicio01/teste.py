
from timeit import timeit

timeit('l.append(0)'),setup='l=[]',number=(00000)
#timeit('l.insert(0,0)'),setup='l=[]',number=(00000)

CLASS LISTA:

def __int__
    self.head=nome

def append(self,valor):
    if tail is none:
        head=tail=No(valor)
    else:
        self tail= self.tail.proximo=No(valor)
